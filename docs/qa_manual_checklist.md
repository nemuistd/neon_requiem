# NEON REQUIEM ブラウザ手動QA記録

## 実行日

2026-05-14

## 実行環境

- OS: Windows
- Node / npm: ローカル `npm.cmd` 実行環境
- 開発サーバー: Vite dev server
- 確認URL: `http://127.0.0.1:5174/`
- ブラウザ操作: 未実行

## 実行したコマンド

```sh
npm.cmd run dev -- --host 127.0.0.1 --port 5174
npm.cmd run build
npm.cmd run check:content
```

補足: 依存関係は既に `node_modules` に存在したため、今回は `npm install` は実行していない。

## 確認結果

| 区分 | 項目 | 結果 | メモ |
| --- | --- | --- | --- |
| 開発サーバー | Vite dev server が起動し、トップページを返す | OK | `Invoke-WebRequest` で HTTP 200 と `<div id="app">` を確認 |
| 初期状態 | 新規セーブで起動できる | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 初期状態 | 表示資源が「灯るさ」になっている | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 初期状態 | 灯るさが0から始まる | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 初期状態 | 毎秒が0.00 / 秒から始まる | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 初期状態 | 注目アイドルに音羽 灯里が表示される | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 初期状態 | 復興区画タブが初期表示される | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| ライブ | 「ライブする」で灯るさが増える | 未確認 | ロジックテストでは `gainManualTomorusa` を確認済み |
| ライブ | メッセージが更新される | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| ライブ | リロード後も灯るさが保存される | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 施設 | 灯るさ10で路地裏ステージを強化できる | 未確認 | ロジックテストでは `upgradeFacility` を確認済み |
| 施設 | 強化で灯るさが消費され、レベルが上がる | 未確認 | ロジックテストでは確認済み |
| 施設 | 毎秒生産と時間経過生産が動く | 未確認 | ロジックテストでは `applyProduction` を確認済み |
| ネオン掲示板 | Lv10到達前は未解放、Lv10で解放される | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 地下礼拝堂 | ネオン掲示板Lv10前は未解放 | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 地下礼拝堂 | 序盤2施設目として出ていない | 未確認 | 定義上はネオン掲示板Lv10後に解放 |
| 歌 | 歌タブを開け、購入後効果が反映される | 未確認 | ロジックテストでは歌の効果を確認済み |
| アイテム | アイテムタブを開け、購入後効果が反映される | 未確認 | ロジックテストではアイテム購入後効果を確認済み |
| アイドル | アイドルタブ、解放条件、注目切替、保存 | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| 記録 | 記録タブ、既読、保存、数値不変 | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |
| オフライン報酬 | `lastSavedAt` 操作による加算と12時間上限 | 未確認 | ロジックテストでは12時間上限を確認済み |
| 設定・デバッグ | 設定ボタン、セーブ削除、`window.__NEON_DEBUG__` | 未確認 | 実ブラウザ操作環境が利用できないため未実行 |

## 発見した問題

- 現在の Codex CLI では `codex.cmd features list` 上、`js_repl` と `js_repl_tools_only` が `removed false`、`browser_use` と `in_app_browser` が `true` である。
- 今後は `node_repl` / `mcp__node_repl__js` を前提にせず、Node REPL MCP server 追加で解決するものとして扱わない。
- JavaScript ロジック検証は `node -e`、`npm run build`、一時スクリプト、Vitest で行う。
- Browser Use は UI、操作、表示、localStorage 挙動の最終確認に限定する。
- Browser Use が Node REPL を要求して止まる場合は、現行 Codex CLI ではその経路は使えないものとして、Chrome + Chrome DevTools Protocol などの代替手段でQAする。
- 実ブラウザ確認が重い場合は、検証項目を小さく分け、チェックリスト化して進める。

## 修正済み / 未修正

- 修正済み: `docs/qa_manual_checklist.md` を追加し、QA実行状況を記録した。
- 修正済み: Vite dev server がトップページを返すことを確認した。
- 未修正: この記録時点の実ブラウザでの手動操作QAは未実行。今後は必要範囲を絞り、Browser Use または Chrome + CDP 代替手段で確認する。
