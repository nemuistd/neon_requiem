# NEON REQUIEM Codex タスク集

このファイルは、Codex に順番に渡すためのタスク文をまとめたもの。

重要: Codex にゲーム全体を一度に実装させない。必ず 1 タスクずつ進める。

---

## Task 0: 資料を読んで計画だけ出す

```text
AGENTS.md と docs/ 以下の資料を読んでください。
まだコードは変更しないでください。

このリポジトリでは、静的ブラウザゲーム NEON REQUIEM を開発します。

現在のリポジトリ構成を確認したうえで、MVP だけを実装する計画を提案してください。

計画には以下を含めてください。
- 推奨ファイル構成
- 現在のプロジェクトに初期化が必要か
- ゲーム状態をどう表現するか
- 自動生産をどう計算するか
- localStorage の保存・読み込みをどう実装するか
- 作成または編集する予定のファイル
- 意図的に実装しないもの
- Codex 側で判断して進めてよい細部
- 実装前にユーザーへ確認すべき大きな判断

まだ実装しないでください。
```

---

## Task 1: プロジェクト初期化

```text
MVP に必要なプロジェクト初期化だけを実施してください。

推奨構成:
- Vite
- TypeScript
- 静的ブラウザアプリ
- バックエンドなし
- 不要な依存関係なし

このタスクでは、まだゲームプレイは実装しないでください。

実装後、以下を報告してください。
- 作成または変更したファイル
- 実行したコマンド
- ローカルでの起動方法
- 次に実装すべきこと
```

---

## Task 2: 初期 UI シェルを作る

```text
NEON REQUIEM の初期 UI シェルを作成してください。

要件:
- タイトル「NEON REQUIEM」を表示する。
- 地下都市をアイドルのライブで復興するゲームであることを短く示す。
- 拍手の表示欄を作る。
- 「ライブする」ボタンを表示する。
- 初期アイドル「音羽 灯里」のカードを表示する。
- 「路地裏ステージ」の施設カードを表示する。
- シンプルな CSS で、暗い地下都市とネオンの雰囲気を出す。

このタスクではロジックは最小限でよいです。
保存、オフライン報酬、複数リソース、複数アイドルは実装しないでください。

実装後、変更ファイルと手動確認方法を報告してください。
```

---

## Task 3: 手動拍手獲得を実装する

```text
手動で拍手を獲得する処理を実装してください。

要件:
- 「ライブする」ボタンを押すと拍手が 1 増える。
- 表示されている拍手量が即時に更新される。
- コードは単純で読みやすくする。
- このタスクでは、自動生産や保存は実装しないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 4: 路地裏ステージ強化を実装する

```text
路地裏ステージの強化を実装してください。

要件:
- 路地裏ステージは Lv 0 から始まる。
- 基本コストは 10 拍手。
- コスト式は floor(10 * 1.15 ^ level)。
- 拍手が足りていれば、強化でコスト分の拍手を消費し、路地裏ステージ Lv を 1 上げる。
- UI に現在 Lv と次の強化コストを表示する。
- 拍手が足りないときは、強化ボタンを無効化するか、強化できないことが明確に分かるようにする。

このタスクでは、必要以上に自動生産や保存を実装しないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 5: 自動生産を実装する

```text
路地裏ステージによる自動拍手生産を実装してください。

要件:
- 生産式は 路地裏ステージ Lv * 0.1 拍手 / 秒。
- UI に秒間拍手量を表示する。
- 時間経過に応じて拍手が増える。
- 完全な 1 秒間隔を仮定せず、タイムスタンプ差分に基づいて計算する。
- シンプルで決定的な実装にする。

このタスクでは保存やオフライン報酬は実装しないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 6: localStorage 保存・読み込みを実装する

```text
localStorage による保存と読み込みを実装してください。

要件:
- 保存キーは neon-requiem-save-v1。
- 拍手、路地裏ステージ Lv、最終保存時刻を保存する。
- 可能ならセーブデータバージョンも保存する。
- ページ開始時にセーブデータを読み込む。
- セーブデータがない場合は初期状態を使う。
- 手動操作後と、プレイ中の一定間隔で保存する。
- 既存の手動獲得、強化、自動生産を壊さない。

このタスクでは、オフライン報酬はまだ実装しないでください。ただし、分離された構造上ごく自然に入る場合は、その理由を説明してください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 7: オフライン報酬を実装する

```text
簡易的なオフライン報酬を実装してください。

要件:
- セーブデータ読み込み時に、最終保存時刻から現在までの経過時間を計算する。
- offlineApplause = applausePerSecond * offlineSeconds で報酬を計算する。
- 計算した報酬を拍手に加算する。
- 「離れている間に、灯里が X 拍手を集めました。」のような短いメッセージを表示する。
- 実装はシンプルにする。

転生、上限、複数リソースなどの将来機能は追加しないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 8: ゲームデータと経済計算を分離する

```text
MVP の実装を、後から拡張しやすいように軽く整理してください。

要件:
- アイドルと施設の定義を、まだ分離していなければデータモジュールに移す。
- コスト計算と生産量計算を分かりやすい関数にする。
- 可能な範囲で、UI 表示とゲーム計算を分ける。
- 既存の挙動は維持する。

新しいアイドル、リソース、施設、システムは追加しないでください。

実装後、変更ファイル、整理した内容、挙動確認方法を報告してください。
```

---

## Task 9: ゲーム性を変えずに雰囲気を改善する

```text
MVP のビジュアル雰囲気と短いフレーバーテキストを改善してください。

要件:
- ゲームロジックは変更しない。
- 暗い地下都市、ネオン、聖歌、アイドルの雰囲気を出す。
- 音羽 灯里と路地裏ステージの短い説明文を整える。
- UI は読みやすく保つ。
- 重いアニメーションや大きなアセットは追加しない。

画像、音声、新キャラ、新リソース、新システムは追加しないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 10: 実装レビュー

```text
現在の実装をレビューしてください。

まだコードは変更しないでください。

以下の観点で確認してください。
- 状態管理
- 保存と読み込み
- オフライン報酬計算
- 自動生産計算
- UI 更新の流れ
- 不要な複雑さ
- 将来の拡張性
- スコープ外機能が混ざっていないか
- Codex が自律的に改善してよい小さな点
- ユーザー確認が必要な大きな点

具体的な問題と推奨修正を報告してください。
```

---

## Task 11: レビューで承認された修正だけを行う

```text
前回レビューで承認された修正だけを適用してください。

新機能は追加しないでください。
デザイン方針を変えないでください。
無関係なリファクタリングはしないでください。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 12: Phase 2 最小拡張を検討する

```text
Phase 2 として、1アイドル・1施設のMVPから、2アイドル・2施設へ拡張できるか確認してください。

まだコードは変更しないでください。

追加候補:
- 新アイドル: 深月 詩乃
- 肩書き: 地下礼拝堂の聖歌係
- 新施設: 地下礼拝堂
- 解放条件: 路地裏ステージ Lv 10

複数リソース、ガチャ、転生、音声、画像は追加しないでください。

GameState、施設定義、セーブデータ移行、UI表示、実装タスク分割を提案してください。
```

---

## Task 13: 2アイドル・2施設の最小実装

```text
Phase 2 の最小実装を行ってください。

要件:
- 深月 詩乃をアイドル定義に追加する。
- 地下礼拝堂を施設定義に追加する。
- 地下礼拝堂は路地裏ステージ Lv 10 で解放する。
- 地下礼拝堂は拍手を直接生産する施設にする。
- GameState は facilities 形式にする。
- v1 セーブデータから v2 セーブデータへ移行できるようにする。
- 複数リソース、ガチャ、転生、音声、画像は追加しない。

実装後、変更ファイル、確認コマンド、手動確認手順を報告してください。
```

---

## Task 14: UIレイアウト方針に合わせて画面を再配置する

```text
docs/neon_requiem_ui_principles.md と ui_sample.png を参考に、現在のUIを実装可能な範囲で再配置してください。

目的はサンプル画像の完全再現ではなく、設計方針を実装可能なレイアウトに落とし込むことです。

要件:
- 上部にタイトル、拍手、毎秒、ライブボタン、設定ボタンを集約する。
- メッセージバーをヘッダー直下に移動する。
- 左側に注目アイドル枠を置く。
- 右側に復興区画のコンテンツ領域を置く。
- 将来のタブ分類を見越した見出しを置く。
- ゲームロジック、保存処理、数値計算は変更しない。
- 画像、音声、新システムは追加しない。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 15: メインメッセージをオートセーブで上書きしない

```text
オートセーブのメッセージが多すぎて、ライブや強化などのメッセージがすぐ消える問題を直してください。

要件:
- オートセーブ自体は維持する。
- オートセーブ成功をメインメッセージに表示しない。
- ライブ、強化、拍手不足、未解放、オフライン報酬のメッセージは従来通り表示する。
- 保存間隔、ゲームロジック、UI文言は不要に変えない。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 16: アイドル切替をデータ駆動にする

```text
注目アイドルが音羽 灯里固定になっている状態を直し、追加済みアイドルを選べるようにしてください。

要件:
- 左側の注目アイドル枠は維持する。
- 下部に小さなアイドル切替リストを追加する。
- IDOLS / IDOL_ORDER を使ってデータ駆動で表示する。
- 未解放アイドルは表示するが選択不可にする。
- 未解放アイドルには解放条件を表示する。
- 注目アイドル選択はまだ保存しない。
- アイドルごとの実効果は追加しない。

実装後、変更ファイルと確認方法を報告してください。
```

---

## Task 17: 開発用デバッグAPIを追加する

```text
localStorage の手動編集が自動保存で上書きされ、機能テストしづらい問題を解決するため、開発用デバッグAPIを追加してください。

要件:
- window.__NEON_DEBUG__.resetSave() で保存データを削除してリロードできる。
- window.__NEON_DEBUG__.setSaveForTest(save) で任意のセーブデータを書き込んでリロードできる。
- テスト用セーブを書き込む直前/直後に自動保存で上書きされないようにする。
- 通常プレイ時の保存処理には影響させない。
- 変更後、Console から使う具体例を示す。

実装後、変更ファイル、確認コマンド、Console例を報告してください。
```
